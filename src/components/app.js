import React, { Component } from 'react';
import Wrapper from './wrapper';

export default class App extends Component {
  render() {
    return (
      <div>
        <h1 className="page-header">Highlights</h1>
        <Wrapper />
      </div>
    );
  }
}
